
doctype html
html
  head
    title= title
    link(rel="stylesheet", href="/style.css")
  body
    h1 City Records
    a(href="/update-city" class="add-btn") ➕ Add New City

    if data.length
      table
        thead
          tr
            th ID
            th Name
            th State
            th Zip Code
        tbody
          each city in data
            tr
              td= city.id
              td= city.name
              td= city.state
              td= city.zipcode
    else
      p No cities found.

    footer
